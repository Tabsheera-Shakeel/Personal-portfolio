/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
           'github.com',
          'i.pinimg.com',
          "www.sortabase.com",
          "media.licdn.com",
          "encrypted-tbn0.gstatic.com"
        ],
    },
};

export default nextConfig;
